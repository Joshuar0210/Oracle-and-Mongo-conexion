/*package com.mongodb;
import com.Car;
import com.example.MongoDBConnection;


public class Main {

    public static void main(String[] args) {
        String connectionString = "mongodb://localhost:27017";
        String dbName = "mydatabase";

        // Crear una conexión a MongoDB
        MongoDBConnection conexion = new MongoDBConnection(connectionString, dbName);

        // Crear un objeto Car
        Car car = new Car("Honda", "Amarillo");

        // Insertar el objeto en MongoDB
        System.out.println("prueba");
        conexion.sendObject(car);

        // Cerrar la conexión
        conexion.closeConnection();
    }
}*/
